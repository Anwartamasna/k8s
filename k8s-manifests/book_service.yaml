apiVersion: v1
kind: Service
metadata:
  name: book-service
  namespace: book-store
spec:
  type: LoadBalancer
  selector:
    app: book-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
